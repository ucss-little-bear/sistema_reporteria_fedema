name: frontend
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.12.0

dependencies:
  excel: ^4.0.6
  file_picker: ^11.0.2
  flutter:
    sdk: flutter
  intl: ^0.20.2
  pdf: ^3.12.0
  printing: ^5.14.3
  provider: ^6.1.5+1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
